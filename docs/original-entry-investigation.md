# 原版入口加载验证记录

## 重启后复验

2026-09-18 14:07 Windows 重启后，用户确认原版窗口正常。原版日志出现主窗口 WebView 初始化完成，此前初始化超时不再复现。未能确定超时的根因，不能把它归因于词典或声称缓存重建解决了问题。

随后重新安装 r3，官方 AppsFolder 入口成功加载中文主界面与商店列表，后台状态 active，首次加载 42 次中文资源。完成退出、原版入口重开、卸载、原版重新启动以及重新安装。卸载后本组件注册表参数和后台进程消失，原版主窗口初始化成功。以下为之前失败尝试的历史记录，不代表重启后的复验结果。

2026-09-18：目标为安装后继续使用官方 Raycast 图标，不新增中文快捷方式。用户接受托盘菜单暂留英文。

当前工作区 r3 是未完成的实验版本，不能作为可用安装包发布。

- 安装器创建普通用户后台，尝试通过应用专属 WebView2 注册表参数连接原版进程。
- HKCU 配置未观察到参数生效；HKLM 配置已出现在 WebView2 命令行，但本机调试端口没有监听，后台始终 waiting、0 页面、0 次中文资源加载。
- 用户反馈看不到窗口，因此没有通过功能验收，未提交或发布 r3。
- 已运行卸载，撤销本次 HKLM/HKCU 应用专属参数、登录启动项和后台组件，恢复插件备份；清理仅限本次测试遗留的 WebView 子进程。
- 已重新调用官方应用入口；原版进程存在且没有汉化调试参数，但进程存在不等于窗口可见，仍需用户确认。

用户随后确认原版仍不可见。日志显示 WebView2 CreateCoreWebView2ControllerAsync 超时（0x800705B4）。备份并重建图形/脚本缓存、临时独立 WebView 用户目录、临时切换已安装的 154.0.4258.18 运行时均未观察到窗口恢复。上述运行时/目录环境变量只作用于诊断进程，没有写入持久环境。所有诊断进程已关闭；没有修改账号数据库、Cookie 或插件设置。

缓存备份在工作区 `.cache/webview-cache-backup`；原始 WebView 账号存储仍在原目录。测试生成的独立目录 `AppData/Local/Raycast-WebView-Recovery` 和 `Raycast-WebView-Recovery154` 保留，没有配置为默认目录。待用户保存其他工作并重启 Windows 后，先验证官方入口能否显示窗口，再继续调查。

不能以安装脚本退出成功或进程存在替代窗口、重开与卸载验收。正式版本仍是需要独立中文入口的 r2，不满足本次新要求。失败的 r3 构建已移出 dist，保存在 `.cache/registry-experiment-unreleased`，禁止发布。
